import React from 'react';
import ReactDOM from 'react-dom/client';
import Tnya from './component/tnya/Tnya'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Tnya/>
  </React.StrictMode>
);

